# 변수----------------------------------------------------------
# 변수가 필요한 이유 : 수많은 빅데이터를 컴퓨터 메모리에 저장해놓고 사용하기 위해


# 변수 선언
# 화살표 기준 왼쪽 : 변수명
# 화살표 기준 오른쪽 : 값
# 5라는 수를 A라는 변수에 저장하라.
A <- 5
A <- 10


# 변수명 규칙
# 1. 영문으로 시작
# 2. 숫자 _ . 사용 가능
# 3. 대소문자 구분
# 4. 한 번 사용한 변수명을 재사용할 수 있음

A.val <- 10
A_val <- 5
a.val <- 20
a_val <- 25
A.val <- 0

B <- 40
B = 50

korea_travel1 <- '서울'
korea_travel2 <- '제주'
KOREA_TRAVEL1 <- '부산'
print(korea_travel1)